# 交接文档 — BTC 双线反转交易机器人项目

> 写给下一个窗口的 Claude。读完这份你就能直接接着干，不用回头翻历史。
> 最后更新: 2026-06-12

---

## 1 · 用户和项目背景

**用户**: 大漂亮 (giantcutie999@gmail.com), 美西 PT 时区
**项目根目录** (Mac): `~/Desktop/交易/` 即 `/Users/guoxiaoquandediannao/Desktop/交易/`
**沙盒路径映射**: `/sessions/modest-zen-euler/mnt/交易/` (沙盒里 git 操作经常因为权限删不掉 .git/*.lock，需要让用户在 Mac 终端跑 `find .git -name "*.lock" -delete`)

**GitHub 仓库**: https://github.com/Jessieeeeai/btc-doublelines-bot (已设为 public, 解决 Actions 2000 min 配额问题)

**Telegram**: 用户的 TG bot 推送策略信号; tg_notify.py 调用 sendMessage API; **没有自动分段, 单条 hard limit 4096 字符**

**cron 触发**: cron-job.org 每 20 分钟触发一次 GitHub Actions, Actions 拉数据→跑 signal_bot_race.py→推 TG→commit state files 回 main 分支

**数据源**: Coinglass API (BTCUSDT 1h K 线)，沙盒访问会 403，需要在 Mac 或 Actions 跑

---

## 2 · 3 策略赛马 (核心架构)

`signal_bot_race.py` 同时跑 3 个策略, 共享同一份信号识别但出场规则不同, 各自 state 文件:

| 策略 | 代码 | 描述 | TP | SL 行为 | 加仓 | 冷却 |
|---|---|---|---|---|---|---|
| A | `state_A.json` | baseline 2R | 2R | 固定 | 无 | 无 |
| B | `state_B.json` | H 阶梯锁 + 连亏 2 停 24h | 8R | +2R→锁+1R, +4R→锁+2R | 无 | 连亏 2 笔暂停 24h |
| C | `state_C.json` | 终极 alpha · Kelly + 金字塔 + 单向冷却 | 8R | H 阶梯锁 | +1R 加 0.5× | 方向感知冷却 |

**仓位约定**: 每单 **$1,000 保证金 + 10x 杠杆 = $10,000 名义仓位** (NOTIONAL_USD = 10000 in signal_bot_race.py)

**R 美元换算**:
```python
dollar_pl = r_value * (r_dollar / entry_price) * NOTIONAL_USD
# r_dollar 是单笔 1R 对应的 BTC 美元距离
```

---

## 3 · 最近改动 (v4 校对修复 — 本次主要工作)

### 修了什么
旧 `build_summary_report` 把 3 策略所有信号拼成一条消息 → 5000+ 字符 → **超 TG 4096 limit → 持仓 section 被截断** → 用户说"没有持仓中的单子"。

### 怎么修的
拆成两个函数 + 改 maybe_send_summary_report 一次发 5 条 TG 消息:

- `build_strategy_section(strategy, state, latest_btc)` — 返回单策略文本 + 累计盈亏
- `send_summary_report(strategies_data, latest_btc, title)` — 拼标题/各策略/横向对比, 用 `send_message` + `time.sleep(0.5)` 分别发
- `REPORT_FORMAT_VERSION = 4` (升一次, 触发首次重发校对)
- 输出顺序: **持仓中 → 已完结 → 作废** (改了顺序, 持仓优先, 即使后面被截也保留持仓)

文件: `signal_bot_race.py` line 770-898 (build_strategy_section / send_summary_report / maybe_send_summary_report)

### 提交记录
- `fe6f943` fix(report): 拆成多条TG消息 (v4) 修复4096字符截断
- 用户在 Mac 跑 `git push` 成功 (commit `a3efadf` 或 `7954e24`)

### 待验证
- 下一轮 Actions cron 触发后, TG 会收到 5 条 v4 校对 (标题 / A / B / C / 横向对比)
- 持仓 section 应该完整, 不再截断

---

## 4 · 当前 PENDING 问题 (用户最新反馈, 未解决)

### 用户原话
> "你这个系统有问题，这几个止盈目标6万4的单子都没有在你的统计里面，你看下"

用户截图: 5/17 20:00 UTC, [A][B][C] 三策略 #001 信号 (空单 @$78,230)，[B] 和 [C] 的 TP 是 **$64,064.88 (8R)**, 用户说统计里看不到这些 8R 目标的单子。

### 诊断结果 (state 数据)
B 和 C 的 #001 在 state 里 **状态确实是 `tp_hit`，但 exit_price = $76,459 (1R 锁), result_r = +1.0**:

```
state_B.json #001:
  entry_price: 78230.0
  tp: 64064.88        ← 8R 目标
  current_sl: 76459.36 ← 阶梯锁后 SL 升到 +1R 锁
  status: tp_hit       ← !!!
  exit_price: 76459.36 ← 实际从 1R 锁出场
  result_r: 1.0
  stair_2r_locked: True
```

### 真正的 bug 是什么 (尚未修)
出场逻辑 (signal_bot_race.py line 371-384) 对空单:
```python
if bar["high"] >= sl:   # 当 BTC 反弹打到 SL (此时 SL 已升至 entry - 1R)
    main_r = (entry - sl) / r    # = +1R (因为 SL 在赢的方向)
    sig_rec["status"] = "sl_hit" if main_r < 0 else "tp_hit"
    # 结果: status="tp_hit", 但其实是 +1R 锁住出场, 不是吃到 8R TP
```

**问题**: H 阶梯锁触发出场后 status 标记为 `tp_hit`, 这在语义上误导:
- 用户/校对报告里 `tp_hit` 显示 🟢 = 赢的止盈
- 但实际是 "+1R 锁止损出场" 不是 "+8R 止盈到达"
- 用户期望的"已完结 8R 单子"和"+1R 锁出场单子"应该分开显示

### 两种修法 (用户没明确选哪个, 需要确认)

**方案 A: 校对里区分显示 "8R 止盈" vs "锁价出场"**
- 不改 status 值, 但在显示时根据 `exit_price == tp` 判断真止盈, 否则显示为"锁价出场 +XR"
- 改 `build_strategy_section` 里的 outcome_icon 逻辑

**方案 B: 加新 status `lock_exit`**
- 出场逻辑判断如果 main_r > 0 且 exit_price != tp 就标 `lock_exit`
- 校对里 `lock_exit` 单独显示 🔒 +1R 这种
- 但要改的地方多: line 167, 240, 348, 377, 407, 463, 525, 688, 759, 777, 815

我建议方案 A — 改动小, 行为更直观。但要等用户确认。

### 单子明细 (state 截图)
```
A 17 个 (TP 2R):
  #001-007 short tp_hit, #008-010 long sl_hit, #011-012 short tp_hit
  #013 short tp_hit, #014-016 long sl_hit, #017 short tp_hit

B 13 个 (TP 8R + 阶梯锁):
  全部 status=tp_hit, 但实际多数是阶梯锁 +1R 出场, 不是吃到 8R
  #008-010 long, 其他 short

C 14 个 (Kelly+金字塔+冷却):
  全部 status=tp_hit
  C 的 #001 还触发了 pyramid_entered (+0.5× 加仓在 76459)
```

state 文件是 6/4 那时的快照 (沙盒拉新 state 不畅), 让用户帮拉最新的 / 看 GitHub raw。

---

## 5 · 关键文件清单

| 文件 | 角色 |
|---|---|
| `signal_bot_race.py` | 主程序: 信号识别 → 出场判定 → 推 TG → 写 state |
| `signals.py` | 双线反转形态识别 (body_ratio + entanglement_tolerance) |
| `backtest.py` | 回测引擎, VariantConfig, run_backtest |
| `tg_notify.py` | TG sendMessage (HTML mode, 15s timeout, **无分段**) |
| `state_A/B/C.json` | 3 策略各自的信号 + 出场状态 + 冷却状态 |
| `state_archive.json` | 旧策略归档 (不再使用) |
| `.env` | COINGLASS_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID |
| `.github/workflows/` | GitHub Actions 配置 |
| `research/` | 各种参数扫描脚本 (tp_sweep_v2.py, find_geyou.py, equity_curve_*.py 等) |
| `策略小白教程.html` | 给非技术读者讲两线反转 (浅米色风格) |
| `F6_视频脚本_v4_结构.html` | 视频脚本结构化 HTML (用 video-script-architect skill 产出) |
| `research/F6_视频脚本_v3.md` / `v4.md` | 视频脚本文本版 |

---

## 6 · 重要数据点 / 不可改的约定

**视频脚本里的核心数字 (来自回测)**:
- 1万 → 18.8万 (+1785%) — 终极 alpha 配置 (Kelly + 金字塔 + 单向冷却)
- 胜率 56%
- $400 投入 → 2.5 周做出来
- "12 月地狱": $7,098 → $1,878 (没加 F6 反共识时的最大回撤)

**TG 消息格式约定**:
- 所有金额用 $ 显示, 不要用 R 单位 (用户明确反复强调)
- 仓位说明: "每单按 $1,000 保证金 + 10x 杠杆 = $10,000 仓位算"
- 每条消息底部带"累计战绩 footer" (胜率/累计R/连胜/冷却状态)

**校对/周报时间**:
- 校对: REPORT_FORMAT_VERSION 升级时立即重发
- 周报: 每周日 12:00 PT (PDT = UTC-7, 周日 19:00-20:00 UTC), 且距上次 ≥ 6 天

---

## 7 · 重要历史 bug + 修复 (避免再踩)

1. **GitHub Actions 配额耗尽** — 私有仓库 2000 min/月 → 改 public 仓库 (unlimited)
2. **Stale 信号 (8 天前的当新信号推)** — detect_new_signals 加 `if sig_bar["ts"] + expire_window_sec < now_ts: continue`
3. **Kelly 总是 1.0×** — build_signal_record 原本在 detect_new_signals 里 batch 调用, 移到 process_strategy 循环里每信号 append + update_signal_status 后调用
4. **Git lock 文件 (沙盒)** — 沙盒删不掉 .git/*.lock, 让用户在 Mac 跑 `find .git -name "*.lock" -delete`
5. **Push 被拒 (non-fast-forward)** — Actions 自动 commit 了 state, 需要 `git pull --rebase` 再 push, 必要时 `git stash`
6. **校对没发** — 实际是被 4096 字符截断, 看似没发但持仓段没了 (已修复 v4)

---

## 8 · 记忆要点 (用户偏好)

- 用户用中文沟通, 不喜欢长篇大论, 偏好简短直接
- 用户不会写 Python, 但会跑命令、看 TG 消息、自己 push 到 GitHub
- 沙盒经常因为权限问题做不了 git push, 让用户在 Mac 上手动跑
- 用户多次强调 "用 $ 不用 R", "校对要包含持仓中", "已完结要单独列每笔明细"
- 当用户问"为啥 1R = 1000?" — 不要简单说"1R 是 risk 单位", 要解释 r_dollar 概念

---

## 9 · 接手后第一件事

1. Read 这份文档 + Read `signal_bot_race.py` (主程序), 重点 line 770-900 (校对相关) 和 line 320-400 (出场判定)
2. Read 最新 state (沙盒里是 6/4 的, 可能要让用户 `git pull` 或读 GitHub raw)
3. 确认下一轮 cron 跑了没 → TG 收到 v4 校对没 → 5 条消息是否都到达
4. 跟用户确认: **方案 A (改显示) 还是方案 B (加新 status)** 来区分"真止盈" vs "阶梯锁出场"
5. 视频脚本 v4 还在迭代中, 用户可能继续给反馈, 用 `video-script-architect` skill

---

## 10 · 工作流约定

- **改代码前**: Read 现有文件, 不要凭印象 Edit
- **跑测试**: 沙盒里 `python3 -c "import ast; ast.parse(open('xxx.py').read())"` 验语法; 完整跑要让用户在 Mac 上跑
- **commit + push**: 沙盒先 `git add` + `commit`, 让用户在 Mac `find .git -name "*.lock" -delete && git pull --rebase && git push`
- **TG 调试**: 沙盒无法访问 TG API (403), 必须用户在 Mac/Actions 跑

---

## 11 · 当前 TaskList

#1 [in_progress] 查 B/C #001 单子为何不在统计 — 已诊断: 不是"不在统计", 而是"显示成 tp_hit 误导, 实际是阶梯锁 +1R 出场"。等用户选方案 A/B。

之前完成的:
- #20 修复校对报告 TG 截断 + 重发 v4 ✓ (commit fe6f943)
- 其他 #1-19 都是历史里完成的策略测试/部署任务

---

写到这。下个窗口的 Claude, 上吧 🚀
